# Minimize Bootstrap and Index Node observation

Public discovery requires no User account or stable User identifier, and Home Nodes never send Taste Profiles, Subscriptions, feedback, or background interest-derived queries to Bootstrap or Index Nodes. Explicit searches are handled without retained product analytics, operators keep only short-lived minimized security logs under a published configurable retention policy, and observed traffic never produces global popularity or personalized ranking.
