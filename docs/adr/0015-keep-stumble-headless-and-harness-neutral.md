# Keep Stumble headless and Agent Harness neutral

Stumble does not provide a dedicated graphical User interface. It exposes structured, transport-neutral capabilities that Agent Harnesses such as conversational assistants and autonomous runtimes can use through adapters such as HTTP, MCP, and CLI; product behavior remains in the core rather than in harness-specific prompts or clients.
