# Ship a removable sponsored Bootstrap Node

New Home Nodes need an initial network contact before peer federation can produce useful Pod discovery. Stumble ships with a sponsored Bootstrap Node configured by default, while allowing the User to remove or replace it and continue through direct Pod URLs or other trusted peers; the Bootstrap Node accelerates reachability but is not authoritative for Pods, trust, quality, or local ranking.
