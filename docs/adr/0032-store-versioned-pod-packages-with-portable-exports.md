# Store versioned Pod Packages with portable exports

SQLite stores authoritative Pod Package versions, and accepted Package Revisions produce signed Pod Events. Agent Harnesses create and propose complete package revisions through structured tools, while directories containing `CONTEXT.md`, `SKILL.md`, source rules, filters, and examples remain the portable import and export format; synchronized remote packages cannot overwrite local Harness Grants or Browser Grants.
