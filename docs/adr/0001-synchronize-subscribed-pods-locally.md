# Synchronize subscribed Pods into the Home Node

When a User subscribes to a remote Pod, the Home Node periodically copies that Pod's public content before assembling the Feed. Ranking and feedback remain local so an Origin Node does not learn the User's interests or reading behavior, and the Feed remains useful when remote nodes are unavailable; synchronized items retain their origin attribution.
